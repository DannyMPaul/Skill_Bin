# ============================================================
# SkillBin Frontend — Environment Variables Template
#
# USAGE: Copy to frontend/.env and fill in values.
#        NEVER commit frontend/.env — it is in .gitignore.
# ============================================================

# URL of the backend API server.
# For local development this is the uvicorn dev server.
VITE_API_URL=http://localhost:8000
