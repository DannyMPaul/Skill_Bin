"""
app/services/__init__.py

Service modules contain all real business logic — matching algorithm,
rating math, Claude API integration. Route handlers stay thin and
delegate to functions here.

Phase 0: empty — services are added as phases build them out.
"""
# Phase 3 will add:  from app.services import resume_parser, rating_seeder
# Phase 5 will add:  from app.services import matching
# Phase 6 will add:  from app.services import rating_updater
