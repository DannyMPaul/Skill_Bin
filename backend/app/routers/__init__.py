"""
app/routers/__init__.py

FastAPI routers are registered here as phases build them out.
Phase 0: empty — no routes yet beyond the health check in main.py.
"""
# Phase 2 will add:  from app.routers import auth
# Phase 3 will add:  from app.routers import employees
# Phase 4 will add:  from app.routers import projects
# Phase 5 will add:  from app.routers import matching
# Phase 6 will add:  from app.routers import outcomes
# Phase 7 will add:  from app.routers import dashboard
